# Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/learning
spring.datasource.username=postgres
spring.datasource.password=funda124
spring.datasource.driver-class-name=org.postgresql.Driver

# Hibernate Configuration
spring.jpa.hibernate.ddl-auto=none
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Liquibase
spring.liquibase.change-log=classpath:db/changelog/db.changelog-master.xml

# Other Spring Configurations
spring.server.port=8080

# Security configuration
spring.security.user.name=user
spring.security.user.password=1

# Jwt secret key
oauth.jwt.secret=ovojebastajnijwtkljuckojinikonecevideti123

# Set Heroku Java version
java.runtime.version=1.8